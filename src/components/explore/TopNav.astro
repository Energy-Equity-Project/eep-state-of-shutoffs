---
import StateSelector from './StateSelector';

interface Props {
  currentCode: string;
}

const { currentCode } = Astro.props;
---

<nav class="flex justify-between items-center pb-4 border-b border-[--color-border-light] mb-6 pr-14">
  <div class="flex gap-5 items-center">
    <span class="text-[13px] text-[--color-ink] border-b border-current pb-0.5">
      Explore by state
    </span>
  </div>
  <div class="min-w-[180px]">
    <StateSelector currentCode={currentCode} client:load />
  </div>
</nav>
