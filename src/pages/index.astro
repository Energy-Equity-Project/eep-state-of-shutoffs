---
import BaseLayout from '../layouts/BaseLayout.astro';
import Section from '../components/Section.astro';
import Introduction from '../components/Introduction.astro';
import Insight from '../components/Insight.astro';
import Conclusion from '../components/Conclusion.astro';
import Resources from '../components/Resources.astro';
import ScrollProgress from '../components/ScrollProgress';
import { insights } from '../data/insights';

const sections = [
  { id: 'intro', label: 'Introduction' },
  ...insights.map((ins) => ({
    id: `insight-${ins.number}`,
    label: `Insight ${ins.number} of 10: ${ins.headline}`,
  })),
  { id: 'conclusion', label: 'Conclusion' },
  { id: 'resources', label: 'Resources' },
];
---

<BaseLayout title="Another World Is Possible — Energy Equity Project">
  <ScrollProgress client:load sections={sections} />
  <main
    data-scroll-container
    class="h-screen overflow-y-auto snap-y snap-mandatory motion-reduce:h-auto motion-reduce:overflow-visible motion-reduce:snap-none"
  >
    <Section id="intro" label="Introduction">
      <Introduction />
    </Section>

    {insights.map((insight) => (
      <Section id={`insight-${insight.number}`} label={`Insight ${insight.number} of 10`}>
        <Insight insight={insight} total={10} />
      </Section>
    ))}

    <Section id="conclusion" label="Conclusion and call to action">
      <Conclusion />
    </Section>

    <Section id="resources" label="Resources">
      <Resources />
    </Section>
  </main>
</BaseLayout>
